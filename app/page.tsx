import {Card,Progress,StatusBadge} from '@/components/ui';import {checklist,documents,disclaimer} from '@/lib/data';
export default function Dashboard(){const completed=8,total=checklist.length,ready=Math.round(completed/total*100),estimated=checklist.reduce((s,i)=>s+Number(i[5]),0);return <div className="space-y-6"><div><h2 className="text-3xl font-bold">Dashboard</h2><p className="mt-2 rounded-xl bg-amber-50 p-4 text-sm text-amber-900">{disclaimer}</p></div><section className="grid gap-4 md:grid-cols-2 xl:grid-cols-4"><Card title="Overall startup readiness" value={`${ready}%`}/><Card title="Completed tasks" value={`${completed}/${total}`}/><Card title="Pending high-priority tasks" value="18"/><Card title="Platforms ready to apply for" value="1"/><Card title="Platforms blocked by missing requirements" value="3"/><Card title="Total estimated startup costs" value={`R${estimated.toLocaleString()}`}/><Card title="Total actual startup costs" value="R0"/><Card title="Missing documents" value={documents.length}/></section><section className="card space-y-4"><h3 className="text-xl font-semibold">Readiness progress</h3>{[['Business registration readiness',43],['Driver licence and permit readiness',30],['Vehicle readiness',22],['Platform registration readiness',18],['Equipment readiness',35],['Tax readiness',25]].map(([l,v])=><Progress key={l as string} label={l as string} value={v as number}/>)}</section><section className="grid gap-4 lg:grid-cols-2"><div className="card"><h3 className="font-semibold">Upcoming deadlines</h3><ul className="mt-3 space-y-2 text-sm"><li><StatusBadge>31 August</StatusBadge> 1st provisional tax reminder</li><li><StatusBadge>End February</StatusBadge> 2nd provisional tax reminder</li><li><StatusBadge>30 September</StatusBadge> Optional top-up reminder</li></ul></div><div className="card"><h3 className="font-semibold">Missing documents</h3><p className="mt-2 text-sm text-slate-600">SA ID/passport, driver licence, PrDP/PDP, proof of address, vehicle RC1, roadworthy, insurance and platform screening documents.</p></div></section></div>}
