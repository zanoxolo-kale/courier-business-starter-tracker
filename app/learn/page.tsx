import {articles} from '@/lib/data';
export default function Learn(){return <div className="space-y-6"><h2 className="text-3xl font-bold">Learning Guide</h2><div className="grid gap-4 md:grid-cols-2">{articles.map((a,i)=><article className="card" key={a}><p className="text-xs font-semibold uppercase text-brand-700">Guide {i+1}</p><h3 className="mt-1 text-lg font-semibold">{a}</h3><p className="mt-2 text-sm leading-6 text-slate-600">Practical South African overview for new independent contractors. Keep separate records, compare platform rules, protect ratings, plan safe shifts, and verify current SARS, municipal, permit, vehicle and platform requirements before applying.</p></article>)}</div></div>}
